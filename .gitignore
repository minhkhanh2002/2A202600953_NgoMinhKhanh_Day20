# Lab artifacts
.venv/
.venv.old/
__pycache__/
*.pyc
.env
.DS_Store

# Native llama.cpp runtime DLLs / installers extracted to repo root (heavy, platform-specific)
/*.dll
/*.exe

# Models — large, downloaded from Hugging Face. Manifest is committed.
models/*
!models/active.json

# llama.cpp source build (bonus track) — heavy, not needed by grader
BONUS-llama-cpp-optimization/llama.cpp/

# Locust raw CSV — screenshot is the evidence, not the CSV
locust_*.csv
benchmarks/locust-*_stats_history.csv
benchmarks/locust-*_exceptions.csv
benchmarks/locust-*_failures.csv

# NOTE: hardware.json AND benchmarks/*.md / *.json files are TRACKED on purpose.
# They are the evidence the rubric grades against. See rubric.md.
